<form method="post" action="{{ path('app_dpt_title_delete', {'id': dpt_title.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dpt_title.id) }}">
    <button class="btn">Delete</button>
</form>
